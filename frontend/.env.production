# Loaded automatically by `vite build` (production mode). Committed on purpose so
# the deployed frontend always points at the real backend, never localhost.
VITE_API_BASE_URL=https://aiagentstudio-portfolio-backend.ia8ype.easypanel.host/api/v1
VITE_APP_NAME="Devinder Portfolio"
VITE_ENABLE_ANALYTICS=true
